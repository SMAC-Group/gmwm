language: r
sudo: required
warnings_are_errors: true

r_packages:
 - ggplot2
 - RcppArmadillo
 - knitr
 - gridExtra
 - grid

before_install:
 - sudo apt-get install -y curl libcurl3 

before_script:
 -  Rscript -e "Rcpp::compileAttributes()"